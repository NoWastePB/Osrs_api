name: Update OSRS Bestiary

on:
  workflow_dispatch:
  schedule:
    - cron: "0 5 * * *"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper
        run: python scripts/scrape_bestiary.py

      - name: Commit updated data
        run: |
          git config user.name "NoWastePB"
          git config user.email "pjheeringa@gmail.com"

          git add data/monsters.json

          if git diff --cached --quiet; then
            echo "No changes detected."
          else
            git commit -m "Update OSRS bestiary data"
            git push
          fi
